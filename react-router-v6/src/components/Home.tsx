import React from 'react';

const Home = () => {
  return (
    <h1 style={{ color: 'red', textAlign: 'center', marginTop: '4rem' }}>
      React Router home page
    </h1>
  );
};

export default Home;
